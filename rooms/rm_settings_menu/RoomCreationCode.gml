// Room Creation Code (room where tutorial should be deactivated)
if (instance_exists(obj_journal_button)) {
    instance_deactivate_object(obj_journal_button); // Deactivate all instances of that object
}
